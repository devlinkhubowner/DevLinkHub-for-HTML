# 📄 チャットツール利用規約（完成版）

本チャットツール（以下「本サービス」）の利用にあたり、以下の規約をご確認ください。本サービスを利用することで、規約に同意したものとみなされます。

---

## 1. サービス提供について
- ブラウザ限定で提供されます。  
- アカウント登録不要で、匿名でサーバーに参加可能。  
- メッセージ送信制限：**1回4,000文字まで、送信間隔5秒以上**。  
- ファイルアップロードは不可。**マークダウン形式テキストのみ対応**。  
- メッセージの検索・編集・削除機能あり。  
- サービス提供期間は未定で、予告なく終了する場合があります。  

---

## 2. GitHub連携（サーバー側で安全に処理）
- ユーザーは **GitHubトークンを直接扱わない**。  
- ユーザーの Gmail や GitHub アカウント情報の取得には **OAuth トークンを使用**し、ブラウザの **クッキーにのみ一時保存**されます。  
- リポジトリへの書き込みやメッセージ管理は、**サーバー側で実行される短期間有効なワンタイムコードを使用**。  
  - このコードは **サーバーで保存されず、処理完了後に破棄**されます。  
  - ユーザー側でトークンを入力する必要はありません。  
- ユーザーの GitHub 情報やメッセージ内容は **サーバーに直接保存されません**。  
- オフラインメッセージなど、必要な一部データは **Supabase に一時的に保存**されます。  
- これにより、GitHubリポジトリへの書き込みや管理は **安全にサーバー側でのみ実行**されます。  

---

## 3. データ保存・プライバシー
- メッセージは **ローカル環境またはサーバー経由で安全に処理**されます。  
- 長期間未アクセスの場合、オフラインメッセージは削除されることがあります。  
- 不適切発言の報告機能は提供されません。  
- 保存されるデータ：`username、nodeid、オフラインメッセージ、サーバー一覧、参加サーバー`。  

---

## 4. サポート・免責
- 問い合わせは公式フォーラムのみで受け付け。  
- サポートは **致命的問題 → 通常問題 → 機能要望 → その他** の順で対応。  
- 本サービスの利用に起因する損害、データ消失、サービス停止について提供者は責任を負いません。  

---

## 5. 規約変更
- 本規約は予告なく変更される場合があります。  
- 変更後にサービスを利用した場合、変更後の規約に同意したものとみなします。  

---

**最終更新日：YYYY/MM/DD**

---

💡 **ポイントまとめ**  
- ユーザー側でトークンを扱わず安全にGitHub APIへアクセス  
- Gmail・GitHub情報取得は OAuth トークンをクッキーに一時保存  
- リポジトリ操作はワンタイムコードでサーバー側安全処理  
- メッセージはローカルまたは安全に管理されたSupabaseに一時保存  
- サービス停止やデータ消失のリスクあり  
