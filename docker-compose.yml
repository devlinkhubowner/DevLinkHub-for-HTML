version: '3.9'

services:
  devlinkhub:
    build: .
    container_name: devlinkhub
    restart: always
    ports:
      - "8001:8001"
    env_file:
      - .env
    working_dir: /app
    command: >
  uvicorn src.main:app
  --host 0.0.0.0
  --port 8001
  --proxy-headers
  --forwarded-allow-ips="*"
